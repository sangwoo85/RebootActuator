spring:
  application:
    name: RebootActuator
server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true
  metrics:
    export:
      defaults:
        enabled:true
